#include <data_frame.h>
#include <describe.h>

int main(int argc, char *argv[]) {
    if (argc != 3) {
        perror("Wrong number of arguments. Usage: logre_train <data_set.csv> <weights.csv>");
        exit(EXIT_FAILURE);
    }

    
}